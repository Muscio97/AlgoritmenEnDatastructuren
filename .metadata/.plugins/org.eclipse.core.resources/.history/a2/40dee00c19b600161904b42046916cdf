'''
Created on 28 nov. 2016

@author: MuscioCraft
'''

temp_str = input("Uw string alstublieft: ")

def mybin(bin_string):
    assert int(bin_string) >=0
    
    if(len(bin_string)<=0):
        return result
    print(bin_string[-1])
    if(int(bin_string[-1])%2==0):
        result =+ '0'
        bin_string[:-1] = bin_string[:-1]-2
    else:
        result =+ '0'
        bin_string[:-1] = bin_string[:-1]-1
        

    return mybin(bin_string)
        
        
print(mybin(temp_str))